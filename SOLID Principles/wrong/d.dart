//our goal is to add new local database like SQLite
class User {
  //User related things
}

class UserManager {
  void saveUserData(User user) {
    //connect to firebase
    //save data
  }

  void saveUserDataToLocal(User user) {
    //connect to SQlite
    //save data to local
  }
}
